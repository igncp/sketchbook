var d = diagrams.box.generateDefinition,
    c = diagrams.box.generateContainer,
    s = diagrams.shared.get;

diagrams.box({
    name: 'tmTheme-Editor concepts chrono',
    body: [
        'The Theme.css_gutter function calculates the bg and fg colors of the gutter (where the line numbers are)',
        'The global function saveAs is provided by the library FileSave.js',
        'plist is an Apple standard: PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd. The properties list, i.e. the XML string of the theme properties.',
        'The bottom bar is really two bars, one from the preview and other from the sidebar.',
    ]
});
