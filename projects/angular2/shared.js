diagrams.shared.set({
  project: 'Angular 2',
  "class-ChangeDetectorRef": "Controls change detection. Allows requesting checks for detectors that rely on observables. It also allows detaching and attaching change detector subtrees.",
});